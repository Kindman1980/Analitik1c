//листинг для обработки.

&НаСервере
Процедура ПрочитатьДанныеХранилищаДанныхНаСервере()
	// Вставить содержимое обработчика.
	Данные = ОбщегоНазначенияБП.ПрочитатьДанныеИзХранилища(объект.Владелец,СокрЛП(Ключи));
КонецПроцедуры

&НаКлиенте
Процедура ПрочитатьДанныеХранилищаДанных(Команда)
	ПрочитатьДанныеХранилищаДанныхНаСервере();
КонецПроцедуры
